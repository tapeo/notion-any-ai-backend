## Change

Slimmed `process.md`: `changes/` entries now use a short template (one-summary line + flat file list, optional one-liner sections) instead of the full report format.

## Files

- `process.md`
