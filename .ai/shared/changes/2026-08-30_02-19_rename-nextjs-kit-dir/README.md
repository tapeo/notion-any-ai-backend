## Change

Renamed `~/projects/agents/nextjs-agent-toolkit` to `~/projects/agents/nextjs-agent-kit` and updated the dispatch table plus VS Code workspace path.

## Decisions

- `dispatch.md` nextjs row now points at `/Users/matteo/projects/agents/nextjs-agent-kit/agents/AGENTS.md`.
- `kits.code-workspace` folder path updated to match.
- The GitHub remote is still `tapeo/nextjs-agent-toolkit.git`; renaming the repo on GitHub is a separate step if wanted. Core README keeps the repo link name as is.
- Historical `changes/` entries keep the old name, intentional.

## Files

- `dispatch.md` (modified)
