## Change

`process.md` template tightened: entries stay short but must include a `## Decisions` section carrying the rules and gotchas future iterations need.

## Decisions

- `## Decisions` is mandatory; it replaces the old "How it works" prose with terse bullets.
- Skip `## Verification` unless the check was non-obvious.
- Existing entries are immutable; the old short-format entry from 02-07 lacks Decisions, treat this entry as the format reference instead.

## Files

- `process.md` (modified)
